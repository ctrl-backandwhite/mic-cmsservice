spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://localhost:9000

    kafka:
        enabled: false
        bootstrap-servers: ${spring.kafka.bootstrap-servers:localhost:9092}

test:
    postgres:
        username: testuser
        password: testpass
        database: cms_test_db

currency-layer:
    api-url: https://api.currencylayer.com/live
    access-key: test-currency-layer-key
