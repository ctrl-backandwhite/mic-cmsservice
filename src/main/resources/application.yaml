server:
    port: 6006

spring:
    application:
        name: mic-cmsservice
    banner:
        charset: UTF-8
    profiles:
        active: ${PROFILE}
    cache:
        type: caffeine
        caffeine:
            spec: maximumSize=500,expireAfterAccess=600s
